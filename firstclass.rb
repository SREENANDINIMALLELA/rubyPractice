# @ instance veriable access inside only that instance /object
#@@ class variables are accessable inside the class
#$ global variables are we can define outside of the method r a class
# creating class
class Person(name , id )
  @name =  name
  @id = id
  #@ name and @id are instance variables attached to the perticular instance /object
end
